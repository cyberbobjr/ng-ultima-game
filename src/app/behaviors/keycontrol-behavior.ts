import {IBehavior} from "../interfaces/IBehavior";

export class KeycontrolBehavior implements IBehavior {
    name = "keycontrol";

    constructor() {
    }

    tick(): any {
        return null;
    }
}